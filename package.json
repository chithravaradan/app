{
  "name": "app",
  "version": "1.0.0",
  "description": "webpage",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "chithra",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1"
  }
}
# This line is added to check the "git pull" command.
# this line is added to check the "git push" command.
